---
title: "Data Mapping and Validation"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Data Mapping and Validation}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = FALSE
)
```

## Load Inputs

```{r setup}
library(fearbase)

data_long <- tibble::tribble(
  ~condition_id, ~study_id, ~participant_id, ~measure, ~value,
  "c1", "s1", "p1", "age", "21",
  "c1", "s1", "p2", "age", "24",
  "c2", "s2", "p3", "age", "29"
)

metadata <- tibble::tribble(
  ~condition_id, ~study_id, ~year,
  "c1", "s1", 2020,
  "c2", "s2", 2021
)
```

## Mapping

`update_mapping()` loads the integrated condition-to-study mapping. By default it
also assigns `mapping` in the global environment for compatibility with older
interactive workflows. Use `assign_global = FALSE` in scripts when you want a
pure return value.

```{r update-mapping}
mapping <- tibble::tibble(
  condition_id = c("c1", "c2"),
  study_id = c("s1", "s2")
)
str(mapping)
```

Most public functions apply the relevant mapping internally, so normal users do
not need to call internal mapping helpers directly.

## Study Lists

`all_studies()` maps metadata and returns sorted study identifiers.

```{r all-studies}
studies <- all_studies(metadata)
head(studies)
length(studies)
```

## Strict Object Lookup

`check_data()` retrieves a named object from an environment and gives explicit
errors when the object cannot be found or when invalid arguments are supplied.
This is useful for OpenCPU-style workflows where object names arrive as strings.

```{r check-data}
example_data <- data.frame(x = 1:5, y = letters[1:5])

check_data("example_data")
check_data("example_data", inherits = TRUE)
```

## Diagnosing Removed Plot Rows

`trace_removed_rows()` helps identify rows that a ggplot layer is likely to drop
because required aesthetics are missing or coordinates fall outside the trained
panel range.

```{r trace-removed-rows}
plot_data <- data.frame(
  x = c(1, 2, NA, 100),
  y = c(1, 3, 4, 5)
)

plot <- ggplot2::ggplot(plot_data, ggplot2::aes(x, y)) +
  ggplot2::geom_point() +
  ggplot2::xlim(0, 10)

trace_removed_rows(plot, data = plot_data)
```

## OpenCPU CSV Loading

`create_csv()` reads an uploaded CSV file and returns a data frame. In local R
scripts, prefer `readr::read_csv()` or `utils::read.csv()` directly. In OpenCPU
workflows, upload the file to `create_csv()` first and pass the resulting session
object to downstream functions.

```{r create-csv}
# In OpenCPU this helper receives the path to the uploaded CSV file.
# uploaded_data <- create_csv(uploaded_file)
```

## JSON Summaries

`json_summary()` converts a named object to a JSON summary. The named object must
be visible to `check_data()`.

```{r json-summary}
# json_summary("example_data")
```
